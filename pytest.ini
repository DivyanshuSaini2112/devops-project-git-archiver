[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    --strict-markers
    -q

markers =
    unit: Unit tests (no network, no filesystem side effects)
    integration: Integration tests (may use local filesystem)
    slow: Slow tests (>5s)
